# Copy to .env.local for local TesterArmy / post-auth testing against preview API.
# After `rishte-api-preview` is live on Cloud Run, paste that URL below.
# Until then, run the backend locally on :3001 with TEST_PHONES set (see TESTERARMY.md).

VITE_API_URL=http://127.0.0.1:3001
